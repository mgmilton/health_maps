<%= form_for @location do |f| %>
  <div class="form-group row">
    <label class="col-sm-2 col-form-label">
      <%= f.label :name %></label>
    <div class="col-sm-10">
      <%= f.select :name, options_for_select(Location.locations.keys) %>
    </div>
  </div>
  <%= f.submit %>
<% end %>
